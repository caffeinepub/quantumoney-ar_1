// Lightweight placeholder — Three.js removed to prevent build OOM.
// The heavy 3D scene has been replaced by CSS-based animations in SpaceBackground.
export default function QuantumUniverseScene() {
  return null;
}
